pub fn encode_url(url: &str) -> String {
    // URL kodlama işlemleri
    url.to_string()
}

pub fn is_token_expired(token: &str) -> bool {
    // Token'ın süresinin dolup dolmadığını kontrol eden yardımcı fonksiyon
    false
}
